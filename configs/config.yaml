defaults:
  - model: wav2vec2
  - data: coral
  - train: default
  - hardware: local
  - _self_

seed: 42
experiment_name: danish-asr-baseline

wandb:
  project: danish-asr
  entity: ""
  mode: online

paths:
  data_dir: data/raw
  output_dir: outputs
  checkpoint_dir: outputs/checkpoints
  log_dir: outputs/logs
